# Milestone 1 validation

Validated locally on September 24, 2026.

- TypeScript strict check and Vite production build pass.
- Six state tests pass: independent SOF levels/on-off, bus and DCA operation during SOF, DCA effective gain/mute without changing member faders, channel selection and processing retention, global solo/reset, fader taper and silence.
- Two Chromium browser tests pass: complete layer/editor/fader/mute/solo/SOF workflow, no uncaught page errors, reset, and desktop control visibility at 1440 × 900 and 1280 × 800.
- Desktop screenshots were reviewed for the mix surface, Input, EQ and SOF presentation. Additional Gate, Compressor and DCA screenshots are generated by the browser test.

Not validated: exact WING firmware behavior, full routing, DSP equivalence, real-console network behavior, audio, Safari/Firefox automated parity or mobile operation. Those are outside this UI milestone.

The standalone offline HTML export was also opened and its Sends on Faders interaction verified without page errors.

Repository: `benhaysmusic/fcc-wing-hub`. Milestone 1 is prepared on `codex/console-ui` for review; no deployment is configured.

## Main group bus mute update

Eight state tests and three browser tests cover the updated console, including independent local/bus mute state, group membership isolation, gently pulsing inherited bus mute, solid direct mute, bus release preserving direct mute, and the reduced-motion alternative. Bus mute indications do not change source fader levels or per-bus sends.
